<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>31-day Flagged Email Review - {{ timestamp_local }}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.2;
            color: #333;
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f5f5f5;
            font-size: 11px;
        }
        .container {
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            padding: 20px;
        }
        h1 {
            color: #0078d4;
            border-bottom: 3px solid #0078d4;
            padding-bottom: 10px;
            margin-bottom: 10px;
            font-size: 24px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 0;
            background: white;
            font-size: 11px;
            line-height: 1.2;
        }
        thead {
            background: #f3f2f1;
        }
        th {
            text-align: left;
            padding: 8px 2px;
            font-weight: 600;
            color: #323130;
            border-bottom: 2px solid #d1d1d1;
            white-space: nowrap;
            font-size: 11px;
        }
        td {
            padding: 2px;
            border-bottom: 1px solid #edebe9;
            vertical-align: top;
            margin: 0;
        }
        tr:hover {
            background-color: #faf9f8;
        }
        .subject-cell {
            font-weight: 600;
            color: #323130;
        }
        .date-cell {
            color: #605e5c;
            white-space: nowrap;
            width: 100px;
        }
        .email-cell {
            color: #0078d4;
            font-family: 'Courier New', monospace;
            font-weight: bold;
            font-size: 15px;
            width: 200px;
        }
        .description-cell {
            color: #605e5c;
            font-style: italic;
        }
        .no-items {
            text-align: center;
            color: #605e5c;
            padding: 30px;
            background: #f9f9f9;
            border-radius: 4px;
            font-style: italic;
        }
        .footer {
            margin-top: 20px;
            padding-top: 10px;
            border-top: 1px solid #e1e1e1;
            text-align: center;
            color: #605e5c;
            font-size: 11px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🚩 31-day Flagged Email Review</h1>
        <p style="color: #605e5c; margin-top: -5px; margin-bottom: 15px; font-size: 11px;">
            Generated: {{ timestamp_local }} | <strong style="color: #d83b01;">{{ total_emails }} flagged emails</strong>
        </p>

        {% if flagged_emails %}
        <table>
            <thead>
                <tr>
                    <th style="width: 100px;">Date</th>
                    <th colspan="2">Subject</th>
                </tr>
            </thead>
            <tbody>
                {% for email in flagged_emails %}
                <tr>
                    <td class="date-cell">{{ email.received_time|format_date }}</td>
                    <td class="subject-cell" colspan="2">{{ email.subject }}</td>
                </tr>
                <tr>
                    <td class="email-cell">{{ email.sender_email }}</td>
                    <td class="description-cell" colspan="2">
                        {% if email.ai_summary %}
                            {{ email.ai_summary }}
                        {% else %}
                            {{ email.body_preview[:200] }}{% if email.body_preview|length > 200 %}...{% endif %}
                        {% endif %}
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        {% else %}
        <div class="no-items">
            <p style="font-size: 14px; margin: 0;">✅ No flagged emails</p>
            <p style="margin: 5px 0 0 0;">No flagged emails found in the last 31 days.</p>
        </div>
        {% endif %}

        <div class="footer">
            <p><strong>31-day Flagged Email Review</strong> - Auto-generated flagged email report</p>
        </div>
    </div>
</body>
</html>
